<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pororing"
        tools:targetApi="31">

        <meta-data android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_ic_notification"/>
        <meta-data android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent"/>
        <meta-data android:name="com.google.firebase.messaging.default.notification_channel_id"
            android:value="fcm_default_channel"/>
        <service
            android:name=".MyFirebaseMessageService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <activity
            android:name=".SettingActivity"
            android:exported="false" />
        <activity
            android:name=".search.PlusActivity"
            android:exported="false" />
        <activity
            android:name=".save.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".converter.ImageConverterActivity"
            android:exported="false" />
        <activity
            android:name=".save.SaveInfoActivity"
            android:exported="false" />
        <activity
            android:name=".myPage.MyPageActivity"
            android:exported="false" />
        <activity
            android:name=".mainPage.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".mainPage.MainActivity"
            android:exported="false" />
        <activity
            android:name=".mainPage.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>